# Bağımlılıklar
node_modules/

# Yapı çıktısı
dist/
.vite/

# Ham arşivler (dokümanlar depoda zaten açık halde)
*.zip
files (1)/

# Editör / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Ortam
.env
.env.local
kale-nobeti-itch.zip

# Uretilen Phaser ozel yapimi (scripts/build-phaser.mjs) - vite.config.ts
# gerektiginde yeniden uretiyor, depoya girmiyor.
node_modules/.phaser-custom/

# kurallar.mjs ve check-bg.mjs TS verisini okumak icin gecici bir vitest
# dosyasi yazip siliyor. Build yarida kesilirse geride kaliyor ve sessizce
# commit'e sizabiliyor (bir kez oldu).
__kural_dokum.test.ts
__bg_dokum.test.ts
